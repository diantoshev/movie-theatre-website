import PageContainer from '../UI/PageContainer/PageContainer';

export default function SeatsPage(props) {
    return <PageContainer> <h1>Page where user will be redirected to select seats in purchase flow</h1></PageContainer>
}